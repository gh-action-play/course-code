name: First Workflow
on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, world!"
      - name: Multiline Commands
        run: |
          node -v
          npm -v
